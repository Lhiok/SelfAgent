{
  "name": "selfagent",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @selfagent/client && npm run build -w @selfagent/desktop",
    "build:electron": "npm run build:electron -w @selfagent/desktop",
    "start:desktop": "npm run start -w @selfagent/desktop"
  },
  "engines": {
    "node": ">=20"
  }
}
